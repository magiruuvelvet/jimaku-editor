/****************************************************
 * AUTOMATICALLY GENERATED BY CMAKE !!              *
 * MODIFICATIONS TO THIS FILE WILL BE LOST !!       *
 ****************************************************/

// don't include this from outside the config target

#ifndef JIMAKU_EDITOR_CONFIG_VERSION_PRIVATE_HPP
#define JIMAKU_EDITOR_CONFIG_VERSION_PRIVATE_HPP

#include <string>

namespace version
{
    // current project version
    const std::string version = "0.8.5";
    const std::string channel = "beta";

    // git version information
    const bool git_has_info = @GIT_RETRIEVED_STATE@;
    const bool git_is_dirty = @GIT_IS_DIRTY@;
    const std::string git_sha1 = "@GIT_HEAD_SHA1@";

    // get full version string, calculated on first execution, than cached
    const std::string &get();
}

#endif // JIMAKU_EDITOR_CONFIG_VERSION_PRIVATE_HPP
